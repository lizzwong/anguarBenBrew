myApp.controller('IngredientController', ['IngredientService','UserService', function (IngredientService, UserService) {
    console.log('InfoController created');
    var self = this;
    self.userService = UserService;
    self.ingredientService = Ingredientservice.
    self.ingredient = IngredientService.ingredient
}]);
